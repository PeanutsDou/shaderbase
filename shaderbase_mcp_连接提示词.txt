帮我连接一个 MCP server：在 MCP 配置文件的 servers 里新增一条，名为 "shaderbase"，type 填 http，url 填 http://<服务器IP>:8001/mcp，enabled 填 true。不要动已有配置。写好后读回确认，然后提示我重启客户端生效。重启后即可查询 G66 shader 代码知识图谱（search_shader/trace_calls/get_code_snippet 等 15 个工具）。
